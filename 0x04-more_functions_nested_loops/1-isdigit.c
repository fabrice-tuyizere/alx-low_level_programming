#include "main.h"
/**
 * _isdigit - it check for output
 * @c: parameter
 *
 * Return: value to be returned
 */
int _isdigit(int c)
{
if ( c >= '0' && c <= '9')
{
	return (1);
}
else
{
	return (0);
}
}
